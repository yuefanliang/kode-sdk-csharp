# Kode.Agent WebApi Assistant
# ==========================
#
# This app exposes an OpenAI-compatible endpoint:
#   POST /v1/chat/completions
# with SSE streaming when `stream=true`.
#

# Optional: protect the API with a simple shared secret.
# If set, requests must include: Authorization: Bearer <KODE_API_KEY>
# KODE_API_KEY=dev-secret

# Where to store agent sessions (JSON store).
KODE_STORE_DIR=./.kode

# Optional: base directory for per-agent data dir (defaults to app ContentRootPath).
# KODE_WORK_DIR=.

# Default provider: anthropic or openai
DEFAULT_PROVIDER=openai

# Anthropic
#ANTHROPIC_API_KEY=your-anthropic-api-key-here
#ANTHROPIC_MODEL_ID=claude-sonnet-4-20250514
# ANTHROPIC_BASE_URL=https://api.anthropic.com
# ANTHROPIC_ENABLE_BETA=false

# OpenAI
OPENAI_API_KEY=sk-kncxkydyjgxgzhbjzfqmyzrlpzntytkpynttsijvybddzyun
OPENAI_MODEL_ID=Pro/zai-org/GLM-4.7
OPENAI_BASE_URL=https://api.siliconflow.cn
# OPENAI_ORGANIZATION=your-org-id

# Agent settings
KODE_SYSTEM_PROMPT=You are a helpful personal assistant.
KODE_TOOLS=fs_read,fs_list,fs_glob,fs_grep
# KODE_PERMISSION_MODE=default|strict|permissive|custom
# KODE_PERMISSION_MODE=default
